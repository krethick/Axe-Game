// Create the Game, right now we need to create a Popup Window
// <> They are inbuilt or built in c++
// " " External Libraries i.e meed to be downloaded seperately.
// raylib is not part of the standard c++ library.
// Functions can have more than one input parameter

#include "raylib.h"
int main()
{
    // Initialise a window
    // Init window takes a width, height that should be in whole numbers and title should be in "" because it is a string.
    // To Run just click on start debugging.
    InitWindow(350,200, "Krethick");
}
