#include "raylib.h"
int main()
{
    // Init window takes a width, height that should be in whole numbers.
    // Whole numbers are integers and cnnot have a fractional part i.e the number should not have decimal places.
    // DateType determines how the value is stored in the memory    
    int width = 350;
    int height = 200;
    InitWindow(width,height, "Krethick");
}
